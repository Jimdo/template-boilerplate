//- Add link-tag to meta to load the variation files
//-
//- @param name   -   set the name of the variation
//- @param file   -   set the file link
//- @param color  -   set the data icon color, used in the Jimdo app as a  
//-                   bullet to differentiate the variations


mixin variation(name, file, color)
  link(rel='stylesheet-variation', data-name=name, href=file, data-icon=color)



//- Add meta-tag with name and content attribute
//-
//- @param name      -   set the name
//- @param content   -   set the content


mixin meta(name, content)
  meta(name=name, content=content)
